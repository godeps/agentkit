# CLI Documentation Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a complete Markdown manual for `cmd/cli` that matches the current implementation and verified runtime behavior.

**Architecture:** Keep the work documentation-only. Add a new `docs/cli.md` that mixes quick-start examples with a complete reference section, then add a discoverability link from the root `README.md`. Validate the guide against `--help` output and a few representative runtime commands.

**Tech Stack:** Markdown, Go CLI help output, existing project docs.

---

### Task 1: Draft the CLI manual

**Files:**
- Create: `docs/cli.md`

**Step 1: Draft the structure**

Include sections for:
- overview
- quick start
- input resolution
- interactive mode
- non-interactive modes
- config and directory layout
- skills and MCP
- sandbox backends
- flags reference
- troubleshooting

**Step 2: Fill with verified examples**

Use command examples based on the current CLI:
- `go run ./cmd/cli`
- `go run ./cmd/cli --prompt "hello"`
- `echo "hello" | go run ./cmd/cli`
- `go run ./cmd/cli --prompt "hello" --stream --stream-format rendered`
- `go run -tags govm_native ./cmd/cli --sandbox-backend=govm --repl`

**Step 3: Review for scope discipline**

Keep the guide limited to CLI usage. Do not expand into SDK API usage or unimplemented future plans.

### Task 2: Add discoverability from the root README

**Files:**
- Modify: `README.md`

**Step 1: Add a documentation link**

Add a short pointer in the CLI-related section linking to `docs/cli.md`.

**Step 2: Keep the change minimal**

Do not rewrite the README. Only add enough to make the CLI manual easy to find.

### Task 3: Verify the document against the implementation

**Files:**
- Test: `docs/cli.md`
- Test: `README.md`

**Step 1: Check help output**

Run: `go run ./cmd/cli --help`
Expected: flags and descriptions match the reference section.

**Step 2: Check interactive mode**

Run: `printf '/quit\n' | go run ./cmd/cli --repl`
Expected: starts REPL and exits cleanly.

**Step 3: Check prompt mode**

Run: `go run ./cmd/cli --prompt "hello"`
Expected: command reaches runtime execution path; any failure should be model/provider related rather than usage parsing.

**Step 4: Check docs-only regressions**

Run: `go test ./cmd/cli/... -count=1`
Expected: PASS
